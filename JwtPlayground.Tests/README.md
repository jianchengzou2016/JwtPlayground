# JwtPlayground.Tests

This project contains unit tests for the JWT playground.

## What it covers

- `TokenService` response shape and JWT contents.
- Public-key export formatting and round-trip RSA import behavior.

## Notes

- These are unit tests, not end-to-end integration tests.
- The tests focus on stable seams that do not require booting the web applications.
